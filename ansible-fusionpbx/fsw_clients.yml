---
# Here are all of our clients, each matching one tenant of the PBX.
#
# The name (e.g. "client1" here) is the id of the tenant, that is the part
# preceding "default_domain".
#
# To force reconfiguration (e.g. to import new agents), set a new value in "refresh"
# and run the script again.
#

clients:
  tenant1:
    url:   "https://stats.srv.my/t1popk/"
    login: "webqloader"
    pass:  "pippo"  
    actions: True
    disabled: False
    av_secret: "x1x1secret"
    refresh: ""

  tenant2:
    url:   "https://stats.srv.my/t2popk/"
    login: "webqloader"
    pass:  "pluto"  
    actions: True
    disabled: False
    av_secret: "x2x2secret"
    refresh: ""

